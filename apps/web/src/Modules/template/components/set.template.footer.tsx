import React from 'react';

import { InputField } from '@finnoto/design-system';

const SetTemplateFooter = () => {
    return (
        <InputField placeholder={'Enter Text'} label='Footer' maxLength={60} />
    );
};

export default SetTemplateFooter;
